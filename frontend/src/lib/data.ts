// Đây là file cũ dùng cho mock data
// Hiện tại đã chuyển toàn bộ mock data sang thư mục mocks
// Hãy import từ thư mục mocks thay vì sử dụng file này

import { products } from '../mocks/products';

export { products }; 