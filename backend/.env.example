# Server configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# JWT configuration
JWT_SECRET=your_jwt_secret_key_should_be_replaced_in_production
JWT_EXPIRES_IN=7d

# Session configuration
SESSION_SECRET=your_session_secret_key_should_be_replaced_in_production

# Redis configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# SQL Server configuration
DB_HOST=localhost
DB_PORT=1433
DB_NAME=ecommerce
DB_USER=sa
DB_PASSWORD=your_strong_password

# Logging configuration
LOG_LEVEL=debug 